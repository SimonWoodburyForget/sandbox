use trivial::*;

fn main() {
    // analyze(include_str!("../inputs/yahtzee-upper-1.txt"));

    // let mut results = [0; 5];
    // let dice = [1, 1, 3, 1, 1];
    // println!("{:?}", yahtzee_vec(&dice));
}
